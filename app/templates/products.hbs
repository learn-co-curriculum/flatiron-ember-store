<div class="container col-md-4" style="margin-left: 50px;">
  
  <h2 style="text-align: center;">Products</h2>
  
  <ul class="list-group">
  {{#each model as |product|}}
    <li class="list-group-item active">
      <a><h4 {{action 'showMore' product}}>{{product.name}}</h4></a> 
    </li>
  {{/each}}
  </ul>

</div>

{{show-product product=currentProduct}}
   
